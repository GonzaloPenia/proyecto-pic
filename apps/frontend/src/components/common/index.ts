/**
 * Archivo barrel para exportaciones centralizadas de componentes comunes.
 * Facilita las importaciones permitiendo importar múltiples componentes desde un solo lugar.
 *
 * Ejemplo de uso:
 * import { Navbar, ProtectedRoute, Hourglass } from '../components/common';
 */

export { default as Navbar } from './Navbar';
export { default as ProtectedRoute } from './ProtectedRoute';
export { default as Hourglass } from './Hourglass';
